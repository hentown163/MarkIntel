NexusPlanner has been deployed successfully!

🚀 Release Information:
  Name: {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}
  Version: {{ .Chart.AppVersion }}

📊 Components Deployed:
  - Backend API: {{ .Values.backend.replicaCount }} replicas
  - Frontend: {{ .Values.frontend.replicaCount }} replicas
  - AI Provider: {{ .Values.app.llmProvider }}

{{- if .Values.ingress.enabled }}
🌐 Application URLs:
{{- range .Values.ingress.hosts }}
  - https://{{ .host }}
{{- end }}
{{- else }}
⚠️  Ingress is disabled. To access the application:
  
  Backend API:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "nexusplanner.fullname" . }}-backend 8000:8000
  
  Frontend:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "nexusplanner.fullname" . }}-frontend 8080:80
{{- end }}

📝 Next Steps:
1. Verify deployments:
   kubectl get pods -n {{ .Release.Namespace }}

2. Check application health:
   kubectl get pods -n {{ .Release.Namespace }} -w

3. View logs:
   kubectl logs -n {{ .Release.Namespace }} -l app=nexusplanner,component=backend --tail=100

{{- if eq .Values.app.llmProvider "bedrock" }}
4. Verify AWS Bedrock access:
   - Ensure IRSA is configured: {{ .Values.app.aws.iamRoleArn }}
   - Model: {{ .Values.app.aws.bedrockModelName }}
   - Region: {{ .Values.app.aws.region }}
{{- end }}

{{- if .Values.database.external.enabled }}
5. Verify database connectivity:
   kubectl exec -n {{ .Release.Namespace }} -it deploy/{{ include "nexusplanner.fullname" . }}-backend -- python -c "from app.infrastructure.config.database import engine; print('✓ Database connected')"
{{- end }}

For more information, visit: https://github.com/yourorg/nexusplanner
